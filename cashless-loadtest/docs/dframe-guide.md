# دليل استخدام DFrame - اختبار الحمل

## 🔄 ماذا يعني "Waiting command start"؟

عندما تشغل Worker بهذا الأمر:
```powershell
.\CashlessLoadTest.Worker.exe http://127.0.0.1:7313 --vp 20 --url https://mada21.com:2401 --run-id TEST
```

الـ Worker **يتصل بـ Controller وينتظر أوامرك!**

```
info: DFrame.DFrameWorkerEngine[0]
      Waiting command start.  ← ينتظر أن تضغط EXECUTE في الواجهة!
```

**هذا طبيعي 100%** - الـ Worker لن ينتهي من تلقاء نفسه، بل ينتظر أوامرك من الواجهة.

---

## 🖥️ شرح واجهة Controller

### الأوضاع الأربعة (Tabs)

| الوضع | الوصف |
|-------|-------|
| **Request** | تنفيذ عدد محدد من الطلبات (Total Request) |
| **Repeat** | تكرار كل worker عدد محدد من المرات |
| **Duration** | التشغيل لمدة زمنية محددة (ثواني/دقائق) |
| **Infinite** | التشغيل للأبد حتى تضغط CANCEL |

---

### الإعدادات الرئيسية

#### 1️⃣ Concurrency (التزامن)
**عدد الطلبات التي تُنفذ بالتوازي في نفس اللحظة**

| القيمة | المعنى |
|--------|--------|
| `1` | طلب واحد في كل مرة (تسلسلي) |
| `10` | 10 طلبات متزامنة |
| `20` | 20 طلب متزامن ← ضغط أعلى على الخادم |

> **مثال:** إذا كان Concurrency = 5، سيكون هناك 5 طلبات تعمل في نفس الوقت بالضبط.

---

#### 2️⃣ Total Request (إجمالي الطلبات)
**العدد الكلي للطلبات التي ستُنفذ ثم يتوقف الاختبار**

| القيمة | المعنى |
|--------|--------|
| `20` | سيتم تسجيل 20 مستخدم ثم التوقف |
| `1000` | سيتم تسجيل 1000 مستخدم ثم التوقف |
| `1000000` | مليون تسجيل! |

> **ملاحظة:** الاختبار ينتهي تلقائياً بعد إكمال Total Request.

---

#### 3️⃣ Worker Limit (حد العمال)
**كم worker يشاركون في الاختبار**

| القيمة | المعنى |
|--------|--------|
| `No Limit` | استخدام جميع Workers المتصلين |
| `1` | استخدام worker واحد فقط |
| `5` | استخدام 5 workers كحد أقصى |

---

### 📊 المعادلة الكاملة

```
الطلبات الفعلية المتزامنة = Concurrency × عدد Workers المستخدمين
```

**مثالك:**
- Workers متصلين: 1 (شغلت exe واحد)
- `--vp 20`: كل worker له 20 Virtual Process
- Concurrency = 20

**النتيجة:** 20 طلب متزامن

---

## 🔁 هل يمكن تنفيذ EXECUTE أكثر من مرة؟

**نعم!** الـ Worker لا ينتهي، بل يبقى متصلاً وينتظر:

1. ✅ تضغط **EXECUTE** → ينفذ الاختبار
2. ✅ بعد الانتهاء → يعود لحالة "Waiting command start"
3. ✅ تضغط **EXECUTE** مرة أخرى → ينفذ اختبار جديد!

**لإيقاف العامل نهائياً:** اضغط `Ctrl+C` في نافذة الأوامر.

---

## 📈 مثال عملي

```powershell
# تشغيل Worker واحد مع 20 Virtual Process
.\CashlessLoadTest.Worker.exe http://127.0.0.1:7313 --vp 20 --run-id TEST001
```

**في الواجهة:**
- Concurrency: `10`
- Total Request: `100`

**ماذا يحدث:**
1. 10 طلبات تعمل بالتوازي
2. كلما انتهى طلب، يبدأ آخر
3. بعد 100 طلب إجمالي → ينتهي الاختبار
4. Worker يعود للانتظار

---

## 🚀 لتحقيق أقصى حمل

```powershell
# تشغيل 3 Workers في 3 نوافذ مختلفة
# نافذة 1:
.\CashlessLoadTest.Worker.exe http://127.0.0.1:7313 --vp 50

# نافذة 2:
.\CashlessLoadTest.Worker.exe http://127.0.0.1:7313 --vp 50

# نافذة 3:
.\CashlessLoadTest.Worker.exe http://127.0.0.1:7313 --vp 50
```

**في الواجهة ستظهر:** `Worker connections: 3`

**بـ Concurrency = 50:** سيكون لديك 150 طلب متزامن! (50 × 3)
