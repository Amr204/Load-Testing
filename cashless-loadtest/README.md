# CashlessLoadTest - أداة اختبار الحمل

نظام اختبار حمل موزع لتسجيل الممثلين/العملاء باستخدام [DFrame](https://github.com/Cysharp/DFrame).

## 🏗️ البنية

```
CashlessLoadTest.sln
├── CashlessLoadTest.Controller/   # واجهة الويب وإدارة الاختبارات
├── CashlessLoadTest.Worker/       # منفذ الطلبات
│   ├── Common/                    # مساعدات HTTP و Postman
│   └── Scenarios/SignupActors/    # سيناريو التسجيل
└── SignupActors/                  # ملفات Postman للإعدادات
```

## ⚡ التشغيل السريع

### 1. تشغيل Controller
```powershell
cd CashlessLoadTest.Controller
dotnet run
# يفتح على http://localhost:7312
```

### 2. تشغيل Worker
```powershell
cd CashlessLoadTest.Worker
dotnet run -- http://localhost:7313 --vp 10 --run-id TEST001
```

### 3. بدء الاختبار
- افتح http://localhost:7312
- اختر **SignupActors**
- حدد العدد المطلوب
- اضغط **Execute**

## 📱 توليد أرقام الهاتف

يولد النظام أرقام هاتف عشوائية بصيغة:

```
79XXXXXXX (9 أرقام)
```

**الصيغة:** `79` + رقم العقدة (1 رقم) + العداد (6 أرقام)

**أمثلة:** `790000001`, `795123456`, `799999999`

> **ملاحظة:** يمكن توليد ملايين الأرقام الفريدة بفضل العداد التصاعدي ورمز العقدة.

## 📁 ملفات Postman

يقرأ النظام الإعدادات تلقائياً من:

```
SignupActors/
├── *.postman_environment.json   # بيانات الاعتماد (ClientId, AppSecret, etc)
└── *.postman_collection.json    # قوالب الطلبات
```

**المتغيرات المطلوبة:**
| المتغير | الوصف |
|---------|-------|
| `BaseApi` | رابط API الأساسي |
| `ClientId` | معرف OAuth |
| `username` | اسم المستخدم |
| `password` | كلمة المرور |
| `AppId` | معرف HMAC |
| `AppSecret` | سر HMAC |
| `profileId` | معرف الملف الشخصي |

## ⚙️ الخيارات

| الخيار | الوصف | الافتراضي |
|--------|-------|-----------|
| `--vp <n>` | عدد المستخدمين الافتراضيين | 1 |
| `--url <url>` | تجاوز BaseUrl | من Postman |
| `--run-id <id>` | معرف التشغيل للتفرد | تلقائي |

## 📊 النتائج

يعرض Controller:
- **created_ok** - تسجيلات ناجحة
- **verified_ok** - تحققات ناجحة
- **duplicate_errors** - حالات تكرار
- **auth_errors** - أخطاء المصادقة
- **validation_errors** - أخطاء التحقق

## 📋 المتطلبات

- .NET 8.0 SDK
- ملفات Postman مع البيانات الصحيحة
